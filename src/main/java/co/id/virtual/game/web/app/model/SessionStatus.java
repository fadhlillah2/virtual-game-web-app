package co.id.virtual.game.web.app.model;

/**
 * Enum representing the status of a game session in the system.
 */
public enum SessionStatus {
    ACTIVE,
    PAUSED,
    COMPLETED,
    CANCELLED,
    ERROR
}
